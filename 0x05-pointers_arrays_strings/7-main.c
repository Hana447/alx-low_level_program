#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;
	char *new;
	new = "l01234567896547";
    str = "0123456789";
puts_half(new);
    puts_half(str);
    return (0);
}
