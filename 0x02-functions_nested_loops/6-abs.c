#include <stdio.h>
/**
  * _abs - This function return the absolut value of intiger
  *
  * @a :parametr
  * Return: return 0
  */
int _abs(int a)
{
	if (a < 0)
	{
		a = -a;
	}
	else
		a = a;
	return (a);

}
