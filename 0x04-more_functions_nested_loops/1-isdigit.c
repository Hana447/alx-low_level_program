#include "main.h"

/**
  *  _isdigit -this function return 1 if c is number or 0 if it is not
  * Return: return 0
  * @c:par
  */
int _isdigit(int c)
{
	if (c >= 'a' && c <= 'z')
		return (0);
	else
		return (1);
}
